#include "stdio.h"
void solveMazePath(int* startPos, int* goalPos) {
    bool finish = (startPos == goalPos);

    while (! finish){
        int nextX = startPos[0];
        int nextY = startPos[1];
    }
}